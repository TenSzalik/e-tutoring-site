const host = 'http://0.0.0.0:8000/api/'

const urls = {
  user: host + 'user/',
  tutorial: host + 'tutorial/',
  token: host + 'token/',
  token_refresh: host + 'token/refresh/',
}

export default urls
